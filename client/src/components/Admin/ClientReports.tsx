import React from "react";

export default function ClientReports() {
  return (
    <section className="section">
      <h1>Client Reports</h1>
      <p>
        Track client progress over time (weight, measurements, survey
        responses).
      </p>
      {/* TODO: Add table or chart components here */}
    </section>
  );
}
